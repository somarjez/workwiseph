[pytest]
testpaths = tests
python_files = test_*.py
markers =
    etl: heavy/destructive tests that run the full ETL against the database (deselected by default; run with -m etl)
addopts = -m "not etl"
